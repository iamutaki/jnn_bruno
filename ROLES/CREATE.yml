info:
  name: CREATE
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/v1/role"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "name": "role 1"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
