info:
  name: REVOKE
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{host}}/v1/device/revoke"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "udid": "a"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
