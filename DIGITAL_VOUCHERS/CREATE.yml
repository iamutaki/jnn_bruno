info:
  name: CREATE
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/v1/voucher"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "name": "voucher 300",
        "price": 3000,
        "description" : null
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
