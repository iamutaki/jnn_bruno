info:
  name: DISTRICT_CREATE
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/district"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
