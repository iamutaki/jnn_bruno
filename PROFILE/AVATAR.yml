info:
  name: AVATAR
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{host}}/v1/profile/avatar"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
