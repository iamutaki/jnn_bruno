info:
  name: GET
  type: http
  seq: 5

http:
  method: GET
  url: "{{host}}/v1/profile"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
