info:
  name: LISTS
  type: http
  seq: 1

http:
  method: GET
  url: "{{host}}/v1/user"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
