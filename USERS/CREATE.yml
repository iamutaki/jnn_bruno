info:
  name: CREATE
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/v1/user"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "name": "Tester",
        "username": "tester",
        "password" : "tester",
        "roleIds" : [
          "01KTRQEZKWKX59M5D93VSA02R1"
        ]
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
