info:
  name: CREATE
  type: http
  seq: 1

http:
  method: POST
  url: "{{host}}/v1/sub-district"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "districtId" : "01KTQYV829JPC1DA2PQZFRBW7W",
        "name" : "sub 1"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
