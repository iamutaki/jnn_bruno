info:
  name: CREATE
  type: http
  seq: 2

http:
  method: POST
  url: "{{host}}/sub-district"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "district_id" : "01KTPN2CS5M5EJDBDC2N3Z2HDC",
        "name" : "sub 1"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
